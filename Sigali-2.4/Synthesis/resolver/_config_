#!/usr/bin/tcsh
# ----------------------------------------------- 
# This file was automatically generated by cmake. 
# Manual editing not recommended. 
# ----------------------------------------------- 

 setenv SigaliResolver_tdd $SigaliSynthesis_resolver/tdd
 if ( -ef "${SigaliResolver_tdd}/_config_") then
    source ${SigaliResolver_tdd}/_config_
 endif
 setenv SigaliResolver_mem $SigaliSynthesis_resolver/mem
 if ( -ef "${SigaliResolver_mem}/_config_") then
    source ${SigaliResolver_mem}/_config_
 endif
 setenv SigaliResolver_export $SigaliSynthesis_resolver/export
 if ( -ef "${SigaliResolver_export}/_config_") then
    source ${SigaliResolver_export}/_config_
 endif
 setenv SigaliResolver_res $SigaliSynthesis_resolver/res
 if ( -ef "${SigaliResolver_res}/_config_") then
    source ${SigaliResolver_res}/_config_
 endif
